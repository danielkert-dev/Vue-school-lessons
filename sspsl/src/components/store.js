// store.js
import { ref } from 'vue';
// Reference to the components
export const userChoice = ref(null);
export const computerChoice = ref(null);
export const result = ref(null);
export const userScore = ref(0);
export const computerScore = ref(0);
